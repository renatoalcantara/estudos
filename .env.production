# Variáveis aplicadas no build de produção (vite build).
# IDs de analytics são públicos (aparecem no client), então ficam versionados.
# Para sobrescrever no deploy, defina as mesmas chaves nas env vars do projeto.

VITE_GA_MEASUREMENT_ID=G-TVX1Z64QHV

# Microsoft Clarity — Project ID
VITE_CLARITY_PROJECT_ID=x5zieffbiu
